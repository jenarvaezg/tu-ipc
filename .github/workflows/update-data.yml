name: Update IPC Data
on:
  schedule:
    - cron: '0 8 16 * *'  # Día 16 de cada mes, 8:00 UTC
  workflow_dispatch: {}     # Permitir ejecución manual
jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run download-data
      - run: |
          git diff --quiet src/data/ipc-data.json && exit 0
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/data/ipc-data.json
          git commit -m "chore: update IPC data from INE"
          git push
